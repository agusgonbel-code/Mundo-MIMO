const {test,expect}=require('@playwright/test');
async function boot(page){await page.goto('/v2/app-v200.html');await page.waitForFunction(()=>Boolean(window.MundoMimoV2RuntimeV420));}
test('v420 exposes 144 implemented games with unique handlers and no clones',async({page})=>{await boot(page);const r=await page.evaluate(()=>{const x=window.MundoMimoV2RuntimeV420,E=window.MundoMimoV2ExpansionV420;return{implemented:x.implemented,handlers:x.handlers,errors:E.errors,clones:E.cloneGroups}});expect(r.errors).toEqual([]);expect(r.clones).toEqual([]);expect(r.implemented).toHaveLength(144);expect(new Set(r.implemented).size).toBe(144);expect(new Set(r.handlers).size).toBe(144);});
test('action-object vocabulary distinguishes functionally wrong and correct object',async({page})=>{await boot(page);await page.locator('[data-age="1-2"]').click();await page.locator('[data-game="accion-y-objeto"]').click();await page.locator('[data-action-object="zapato"]').click();await expect(page.locator('#feedback')).toContainText('sirve');await page.locator('[data-action-object="vaso"]').click();await expect(page.locator('#feedback')).toContainText('encajan');});
test('count steps enforces one-to-one target',async({page})=>{await boot(page);await page.locator('[data-age="2-3"]').click();await page.locator('[data-game="cuenta-los-pasos"]').click();await page.locator('[data-step]').click();await expect(page.locator('[data-count]')).toHaveText('1');await page.locator('[data-step]').click();await expect(page.locator('#feedback')).toContainText('Contaste');});
test('word families reject semantic intruder then accept full category',async({page})=>{await boot(page);await page.locator('[data-age="3-4"]').click();await page.locator('[data-game="familias-de-palabras"]').click();await page.locator('[data-family="mesa"]').click();await page.locator('[data-check-family]').click();await expect(page.locator('#feedback')).toContainText('intruso');await page.locator('[data-family="mesa"]').click();for(const x of ['gato','perro'])await page.locator(`[data-family="${x}"]`).click();await page.locator('[data-check-family]').click();await expect(page.locator('#feedback')).toContainText('familia');});
test('temporal relation distinguishes before, after and simultaneous',async({page})=>{await boot(page);await page.locator('[data-age="4-5"]').click();await page.locator('[data-game="antes-despues-y-a-la-vez"]').click();await page.locator('[data-temporal="antes"]').click();await expect(page.locator('#feedback')).toContainText('Revisa');await page.locator('[data-temporal="despues"]').click();await expect(page.locator('#feedback')).toContainText('temporal');});
test('forest clues require all constraints',async({page})=>{await boot(page);await page.locator('[data-age="5-6"]').click();await page.locator('[data-game="pistas-del-bosque"]').click();await page.locator('[data-clue="pez"]').click();await expect(page.locator('#feedback')).toContainText('todas');await page.locator('[data-clue="buho"]').click();await expect(page.locator('#feedback')).toContainText('pistas');});
test('advanced v420 games stay out of early bands',async({page})=>{await boot(page);await page.locator('[data-age="0-1"]').click();for(const id of ['familias-de-palabras','antes-despues-y-a-la-vez','pistas-del-bosque'])await expect(page.locator(`[data-game="${id}"]`)).toHaveCount(0);});